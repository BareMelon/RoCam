{
  "name": "feedback-roblox",
  "private": true,
  "workspaces": [
    "api",
    "dashboard",
    "shared"
  ],
  "scripts": {
    "dev:api": "npm run build --workspace @feedback/shared && npm run dev --workspace api",
    "dev:dashboard": "npm run dev --workspace dashboard",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
